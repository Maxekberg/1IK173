public class main{
    public static void main(String[] args) {
        int[] myNum = {10, 20, 30, 40};
        int[] test = {myNum.length + 1};

        System.out.println(myNum[0]);

        /*
         System.out.println(myNum.length);
        for (int i = 0; i < myNum.length; i++){
            test[i] = myNum[i];

        }

            int pos = 2;
            int element = 10;
            for (int i = 0; i < myNum.length; i++) {
                if (myNum[i] == pos) {
                    myNum[i] = element;
                }
        }

         */
    }
}
